<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OutBackX.ViewModel"
             mc:Ignorable="d"
             x:Class="OutBackX.Views.GerirPage"
             NavigationPage.HasNavigationBar="false">

  <ContentPage.BindingContext>
    <vm:GerirViewModel></vm:GerirViewModel>
  </ContentPage.BindingContext>

  <ContentPage.Content>
      <StackLayout Padding="20" BackgroundColor="#f2d5a1">
        <StackLayout VerticalOptions="FillAndExpand">
          <Label Text="Welcome to Gerir"
                 VerticalOptions="CenterAndExpand"
                 HorizontalOptions="CenterAndExpand" />
          <Button Text="Cadastrar" Command="{Binding CadastrarClicked}" BackgroundColor="#e26c32"></Button>
        </StackLayout>
        <StackLayout VerticalOptions="End">
          <Button Text="Voltar" Command="{Binding VoltarClicked}" BackgroundColor="#fe983e"></Button>
        </StackLayout>
      </StackLayout>
    </ContentPage.Content>
</ContentPage>